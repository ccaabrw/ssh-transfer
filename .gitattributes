# Ensure Perl scripts always use Unix (LF) line endings
*.pl text eol=lf
*.pm text eol=lf

# Ensure shell scripts use Unix (LF) line endings
*.sh text eol=lf

# Markdown files should use LF
*.md text eol=lf

# Default behavior for text files
* text=auto
